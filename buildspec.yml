version: 0.2

phases:
  install:
    commands:
      - npm install

  build:
    commands:
      - npm run build

  post_build:
    commands:
      - aws s3 sync build/ s3://amplify-drinkdishapp-dev-30511-deployment

artifacts:
  files:
    - '**/*'
  base-directory: 'build'
